[package]
name = "libra-governor-estimator"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Libra Governor: estimator crate — self-calibrating empirical-quantile preflight estimates from local ExecutionReceipt history."

[dependencies]
libra-governor-domain = { path = "../domain" }
libra-governor-ledger = { path = "../ledger" }
serde.workspace = true

[dev-dependencies]
serde_json.workspace = true
time.workspace = true
