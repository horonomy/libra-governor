[package]
name = "libra-governor-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Libra Governor: protocol crate (placeholder scaffold, no logic yet)."

[dependencies]
