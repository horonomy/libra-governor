# HORO-1127 MVP 1.0 release-gate evidence summary

Generated by `experiments/mvp1_validation/run_corpus.py` and `run_validation_matrix.py`, driving the real compiled `libra-governor` binary as a subprocess (real JSON on stdin, real Unix-socket daemon, real SQLite ledger) — no Rust function called in-process.

## Corpus

- Corpus size: **34**
- Preflight success rate: **100.0%**
- Estimate-present rate: **100.0%**
- Receipt-reconstruction success rate: **100.0%**
- Stop hook safe-outcome rate: **100.0%**
- Tasks with any error: **0**

- Outcome distribution across all finalized receipts: `{'Unknown (no automated completion verification in MVP 1)': 34}`
- Fixture distribution: `{'rust-crate': 12, 'python-pkg': 11, 'ts-pkg': 11}`

## Required validation matrix

| Check | Result |
|---|---|
| `resume_restart` | **PASS** |
| `multi_session_same_context` | **PASS** |
| `user_abort_and_stray_stop` | **PASS** |
| `daemon_restart_mid_flight` | **PASS** |
| `recon_budget_cap` | **PASS** |
| `clean_worktree_after_preflight` | **PASS** |
| `privacy_no_raw_prompt_leak` | **PASS** |
| `concurrency_wal_no_lost_writes` | **PASS** |

### Detail

**resume_restart** — PASS

> receipts before kill=3(exp)/3, after respawn+3more=6(exp)/6, post-kill hooks all succeeded=True

**multi_session_same_context** — PASS

> session A -> task 6cd0a9f0-42cf-445f-a8a0-61852c3b35af; session B -> task 1c7788c2-712a-45ae-b916-1bd948ecec41 (same cwd+prompt). Observed real behavior per HORO-1124's resolve_or_create_task_for_session: each session_id gets its own TaskId; there is no cross-session task merge in MVP 1.0 (documented as out of scope in integrations/claude-code/README.md).

**user_abort_and_stray_stop** — PASS

> preflight_ok=True (session never Stopped, simulating user abort). stray-stop (session with no preceding preflight) safe no-op=True: exit 0, stdout/stderr empty (hook_stop.rs only eprintln!s on the Finalized branch), daemon.log contains the expected 'no active task ... safe no-op' line. daemon still alive/responsive after=True

**daemon_restart_mid_flight** — PASS

> preflight_ok=True, stop after kill exited cleanly=True (no crash), receipts persisted for killed-mid-flight task=0 (expect 0), daemon NOT auto-restarted by hook stop=True (by design: hook_stop.rs uses connect_only, never ensure_daemon_connection), next hook user-prompt-submit DOES auto-respawn the daemon=True

**recon_budget_cap** — PASS

> repo with 2600 files (> ReconBudget::default().max_files=2000): preflight completed in 0.06s (no hang), truncation signal present=True, confidence downgraded (low/medium)=True, raw context='{"hookSpecificOutput":{"additionalContext":"[libra-governor] Preflight complete (task 9de29811-2b32-4fa4-ad4f-f39404ecbe49, confidence: low, recon: 0.00s).\\nDraft Completion Contract (revision 1):\\n  - [required] Matches the user\'s stated request\\nNote: reconnaissance budget exhausted before establi'

**clean_worktree_after_preflight** — PASS

> git status --porcelain scoped to each fixture path, before and after preflight: rust-crate: before='' after=''; python-pkg: before='' after=''; ts-pkg: before='' after=''

**privacy_no_raw_prompt_leak** — PASS

> distinctive nonce 'NONCE-686d30126dcc49119a146e6aacb0d140' grepped (raw bytes) against ledger.sqlite3, ledger.sqlite3-wal, and daemon.log. Found in: none (clean)

**concurrency_wal_no_lost_writes** — PASS

> fired 25 concurrent `hook post-tool-use` subprocesses against one session (OS-level concurrent connects to the daemon's serial single-threaded accept loop, exercising WAL + busy_timeout=5000ms from crates/ledger/src/store.rs); final tool_call_count recorded on the receipt = 25 (expected 25)

