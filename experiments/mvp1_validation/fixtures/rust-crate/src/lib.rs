// Synthetic fixture crate for the HORO-1127 release-gate validation
// corpus (experiments/mvp1_validation). Not part of the Cargo workspace
// — recon only ever reads this tree, never builds it.

pub mod login;
pub mod pagination;
