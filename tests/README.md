# Integration tests

Cross-crate and end-to-end integration tests for the Governor (e.g.
daemon <-> hooks lifecycle scenarios, ledger consistency checks).

This directory is currently a placeholder established by the bootstrap
ticket (HORO-1118). Per-crate unit tests live inside each crate under
`crates/*/src`; real integration tests land here in follow-up tickets.
