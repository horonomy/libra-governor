# Schemas

Wire-format and storage schemas for the Governor (protocol messages,
ledger records, estimator inputs/outputs).

This directory is currently a placeholder established by the bootstrap
ticket (HORO-1118). Real schema definitions land alongside the
`crates/protocol` and `crates/ledger` implementations in follow-up
tickets.
